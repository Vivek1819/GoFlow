# GoFlow

GoFlow is a distributed background job execution engine written in Go.

It allows applications to submit tasks that are executed asynchronously by worker processes using a Postgres-backed job store.